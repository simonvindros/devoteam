import React, { FC } from "react";
import { Wrapper, HeaderText } from "./styled";

export const Header: FC = () => {
  return (
    <Wrapper>
      <HeaderText>Meet the Team</HeaderText>
    </Wrapper>
  );
};
